# Start Redis for the queue. Run: docker compose up -d
# Then start Celery worker and FastAPI locally (see README).

services:
  redis:
    image: redis:alpine
    container_name: financial-analyzer-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
